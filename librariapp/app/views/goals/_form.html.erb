<%= form_for @goal, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :nombreVendedor, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :nombreVendedor, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :mes, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :mes, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                goals_path, :class => 'btn' %>
  </div>
<% end %>
